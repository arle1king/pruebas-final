[pytest]
testpaths =
    manejadorDisponibilidad/src/tests
    manejadorConfidencialidad/src/tests
    manejadorIntegridad/src/tests
python_files = test_*.py
addopts = -q
markers =
    integration: tests que dependen de AWS o endpoints reales
    unit: tests unitarios puros
